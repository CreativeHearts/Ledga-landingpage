import Image from "next/image";

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-between p-24">
      {/* Main Screen Board */}

      <div>contains an image with overlayed text</div>

      {/* Industries */}
      <div>
        A row of images with icons but will however be different in phone size
      </div>

      {/* Benefits */}
      <div>
        A row of images with icons but will however be different in phone size
      </div>
    </main>
  );
}
